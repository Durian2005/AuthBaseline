# .NET build output
**/bin/
**/obj/
**/publish/

# Frontend dependencies and build output
**/node_modules/
AuthClient/dist/

# Rust/Tauri build output
src-tauri/target/

# Local IDE and OS files
.vs/
.vscode/
.idea/
.DS_Store
Thumbs.db

# WorkBuddy local project state
.workbuddy/

# Local runtime and secret configuration
.env
.env.*
**/appsettings.Development.json
**/appsettings.Local.json

# Generated installers and local test packages
*.exe
*.msi
*.zip
pkg_src/
test_install/
_old_dist_bak/
_old_dist_old_bak/

# Diagnostic logs and screenshots
*.log
screen.png
tools/buildlogs/

# Local database backups（含真实用户数据，仅用于误删回滚，绝不入库）
tools/backup/

# 数据库导出与哈希链快照 —— 这些是**数据**，不是源码，一律不入库：
#   full_db_backup.json                含 bcrypt 口令哈希、明文会话 ticket、
#                                      真实绑定邮箱，以及 800+ 条带 request/response 报文的审计记录
#   chain_snapshot_*.json              含真实姓名与审计原文
# 一条 `git add -A` 就会把整库快照推上去，所以规则必须在第一次 push 之前生效。
# 整个 tools/evidence/ 一律不入库 —— 它装的是"运行证据"，不是源码：
#   截图：界面上显示着真实账号名（审计页 / 用户管理页的账号列），
#         没法像文本那样扫描并脱敏，唯一的办法就是不传；
#   快照：chain_backup_before_repair.json 是 322 条真实操作日志的导出，
#         还带着本机浏览器 UA 与账号名 —— 那是"数据"，不是源码。
# 注意：tools/*.png 只覆盖 tools/ 第一层，管不到 tools/evidence/，
#       所以这一条必须按目录写死；否则 git rm --cached 之后，
#       一条 git add -A 又会把它们加回来。
tools/evidence/

# 本地验证脚本跑出来的截图同样不入库（理由同上）。
# 需要某张截图作为文档时，把它移到 docs/ 下，或用 git add -f 单独放行。
tools/*.png
